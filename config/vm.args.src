## Node identity and runtime flags for mcl_bookclub_observer.
-name ${MCL_NODE_NAME}@${MCL_NODE_HOST}
-setcookie ${MCL_COOKIE}

+K true

## Async thread pool. Raise it only if this service does real file or port I/O.
+A 8
